{% extends 'base.html.twig' %}

{% block title %}Game{% endblock %}

{% block body %}
    <h1>Game</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game.id }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ game.result }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_game_crud_index') }}">back to list</a>

    <a href="{{ path('app_game_crud_edit', {'id': game.id}) }}">edit</a>

    {{ include('game_crud/_delete_form.html.twig') }}
{% endblock %}
